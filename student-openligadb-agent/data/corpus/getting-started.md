# Bundesliga Agent Knowledge Base

This local markdown context is optional and complements live OpenLigaDB data.

Use this for "static" explanations such as:

- What does `bl1` mean?
- How are points counted in a table?
- What is the difference between finished and scheduled matches?
- Which season does 2025 represent in OpenLigaDB?

OpenLigaDB tools should still be used for live matches and standings.
